/**
 * The application's identity line, in the one place that decides it.
 *
 * A reader meets it on the panorama, on a case study, on a field note, on the
 * note index and on the résumé, and a line that reads one thing on four of them
 * and something else on the fifth is a line that stops being an identity. Every
 * surface asks here, so they cannot disagree.
 *
 * It lives beside the panels rather than in `components/metro` because the
 * status bar is a Windows Phone part and this is what *this* application writes
 * on it.
 */
export const APP_IDENTITY = "AJMAL / PORTFOLIO";
